general {
    lock_cmd = pidof hyprlock || hyprlock       # dbus/sysd lock command
    before_sleep_cmd = loginctl lock-session    # lock before suspend
    after_sleep_cmd = hyprctl dispatch dpms on  # wake up display
}

# The user requested no automatic screen-off.
# I am commenting out the auto-lock and power-off listeners.
# You can still lock manually with SUPER + L.

# listener {
#     timeout = 300                                # 5min
#     on-timeout = loginctl lock-session           # lock screen
# }

# listener {
#     timeout = 330                                # 5.5min
#     on-timeout = hyprctl dispatch dpms off       # screen off
#     on-resume = hyprctl dispatch dpms on         # screen on
# }

# listener {
#     timeout = 1800                               # 30min
#     on-timeout = systemctl suspend                # suspend pc
# }
